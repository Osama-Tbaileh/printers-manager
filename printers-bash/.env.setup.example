################################################################################
# PRINTER SERVER SETUP CONFIGURATION
# 
# Copy this file to .env.setup and configure your settings
# 
#   cp .env.setup.example .env.setup
#
# This file will be loaded by usb_setup.sh script
################################################################################

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# GITHUB REPOSITORY SETTINGS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# GitHub repository (format: owner/repo)
GITHUB_REPO="iztech-team/printers-manager"

# GitHub Personal Access Token (only needed for PRIVATE repos)
# Get token from: https://github.com/settings/tokens
# Required permissions: repo (Full control of private repositories)
GITHUB_TOKEN=""

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SERVER SETTINGS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Installation directory on the Raspberry Pi
INSTALL_DIR="$HOME/printer-server"

# Server port (default: 3006)
SERVER_PORT="3006"

# Logo filename (must exist in the repo)
LOGO_FILENAME="BarakaOS_Logo.png"

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# TAILSCALE REMOTE ACCESS SETTINGS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Tailscale Auth Key for automatic connection
# 
# Get your auth key from: https://login.tailscale.com/admin/settings/keys
# 
# Click "Generate auth key" and select:
#   ✓ Reusable (recommended - can use on multiple devices)
#   ✓ Ephemeral (optional - device auto-removes when offline)
#   ✓ Set expiration (90 days, 180 days, or never expire)
# 
# Example:
# TAILSCALE_AUTH_KEY="tskey-auth-k8A9B7C6D5E4F3G2H1-ABCDEFGHIJ1234567890"

TAILSCALE_AUTH_KEY=""

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SECURITY BEST PRACTICES FOR RESTAURANT DEPLOYMENTS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# ✓ RECOMMENDED: Use EPHEMERAL keys
#   - Devices auto-remove from network when disconnected
#   - Keys can't be reused after first connection
#   - Even if Pi is stolen, key is useless
#   - Set 90-day expiration for additional security
#
# ✗ AVOID: Reusable non-expiring keys
#   - Can add unlimited devices to your network
#   - If USB/Pi stolen, must manually revoke key
#
# SECURITY CHECKLIST:
#   □ Use ephemeral auth keys
#   □ Set key expiration (90 days recommended)
#   □ Never commit .env.setup to Git
#   □ Enable MFA on Tailscale account
#   □ Use separate keys per restaurant location
#   □ Regular audit of connected devices
#   □ Enable device authorization in Tailscale
#
# IF DEVICE IS STOLEN/LOST:
#   1. Go to https://login.tailscale.com/admin/machines
#   2. Find device and click "Delete"
#   3. Revoke the auth key at /admin/settings/keys
#   4. Device loses network access immediately
#
# KEY STAYS ON PI - IS IT SAFE?
#   YES, if using EPHEMERAL keys:
#   - Key only works once during initial connection
#   - After connection, key is useless (can't add more devices)
#   - Script deletes standalone key files after use
#   - Only stored in .env.setup on USB (not in Git)
#   - Tailscale provides remote device management
#
#   MEDIUM RISK if using REUSABLE keys:
#   - Can be used to add devices to your network
#   - Must revoke manually if compromised
#   - Better for testing, not production
#
################################################################################